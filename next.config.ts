import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,

  
};
module.exports = {
  output: 'export',
  basePath: '/aymanglobalconnect',
  assetPrefix: '/aymanglobalconnect/',
};

export default nextConfig;
